# PostgreSQL
DB_NAME=shop_portfolio
DB_USER=postgres
DB_PASSWORD=replace_with_your_local_password
DB_HOST=localhost
DB_PORT=5432

# Authentication: use a long random value (32+ characters recommended)
JWT_SECRET=replace_with_a_long_random_secret_value

# Server
PORT=3500
CLIENT_ORIGIN=http://localhost:3000
